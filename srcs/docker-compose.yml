version: "3.9"                              # 3.9 is stable version of DOCKER-COMPOSE

services:
  ft_mariadb:
    build: ./requirements/mariadb
    container_name: inception_mariadb
    volumes:
      - mariadb_data:/var/lib/mysql
    networks:
      - inception
    env_file:
      - ../secrets/.env

  ft_wordpress:
    build: ./requirements/wordpress
    container_name: inception_wordpress
    volumes:
      - wordpress_data:/var/www/html
    depends_on:
      - ft_mariadb
    networks:
      - inception
    env_file:
      - ../secrets/.env
    
  ft_nginx:
    build: ./requirements/nginx
    container_name: inception_nginx
    ports:
      - "443:443"
    volumes:
      - wordpress_data:/var/www/html
    depends_on:
      - ft_wordpress
    networks:
      - inception
    env_file:
      - ../secrets/.env

volumes:
  mariadb_data:
    driver: local
  wordpress_data:
    driver: local

networks:
  inception:
    driver: bridge